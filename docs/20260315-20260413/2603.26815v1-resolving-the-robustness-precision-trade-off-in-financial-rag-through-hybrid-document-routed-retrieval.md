---
title: Resolving the Robustness-Precision Trade-off in Financial RAG through Hybrid Document-Routed Retrieval
title_zh: 通过混合文档路由检索解决金融 RAG 中的鲁棒性与精确度权衡
authors: "Zhiyuan Cheng, Longying Lai, Yue Liu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.26815v1"
tags: ["query:bk-llm"]
score: 9.0
evidence: 金融RAG系统的混合文档路由检索
tldr: 针对金融文档RAG中分块检索（CBR）易产生跨文档混淆而语义文件路由（SFR）精度不足的问题，本文提出混合文档路由检索（HDRR）。该方法采用两阶段架构，先利用SFR筛选目标文档，再在文档内进行分块检索。实验表明，HDRR在FinDER基准上显著提升了检索精度并降低了失败率，有效解决了鲁棒性与精确度之间的权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26815v1/fig-001.webp\", \"caption\": \"Table 1\", \"page\": 11, \"index\": 1, \"width\": 1029, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2603.26815v1/fig-002.webp\", \"caption\": \"Figure 1: Chunk-Based RAG (CBR) pipeline. The offline phase indexes documents into dual stores (FTS5 for keyword search,\", \"page\": 7, \"index\": 2, \"width\": 927, \"height\": 1262}, {\"url\": \"assets/figures/arxiv/2603.26815v1/fig-003.webp\", \"caption\": \"Table 6\", \"page\": 14, \"index\": 3, \"width\": 498, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2603.26815v1/fig-004.webp\", \"caption\": \"Figure 2: Semantic File Routing (SFR) pipeline. No offline\", \"page\": 8, \"index\": 4, \"width\": 498, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2603.26815v1/fig-005.webp\", \"caption\": \"Figure 4: Side-by-side comparison of the CBR and SFR\", \"page\": 12, \"index\": 5, \"width\": 498, \"height\": 879}, {\"url\": \"assets/figures/arxiv/2603.26815v1/fig-006.webp\", \"caption\": \"Figure 3: Hybrid Document-Routed Retrieval (HDRR) pipeline. Stage 1 routes the query to the correct document via LLM\", \"page\": 9, \"index\": 6, \"width\": 825, \"height\": 1306}]"
motivation: 传统的金融RAG系统在处理结构相似的语料时，分块检索容易导致跨文档混淆，而全文档路由则牺牲了检索的精确度。
method: 提出HDRR两阶段架构，首先通过LLM结构化输出将查询路由至特定文档，随后在限定文档范围内执行分块检索。
result: "HDRR在FinDER基准测试中取得了7.54的最高平均分，失败率降至6.4%，且完美回答率提升至20.1%。"
conclusion: HDRR通过结合文档级过滤与块级检索，成功解决了金融RAG中的鲁棒性-精确度权衡问题，实现了性能的全面提升。
---

## 摘要
用于金融文档问答的检索增强生成 (RAG) 系统通常遵循基于分块 (chunk-based) 的范式：将文档拆分为片段，嵌入到向量空间中，并通过相似度搜索进行检索。虽然这种方法在通用场景下有效，但在结构同质的语料库（如监管文件）中会遇到跨文档分块混淆的问题。语义文件路由 (SFR) 利用大语言模型 (LLM) 的结构化输出将查询路由至整个文档，虽然减少了灾难性失败，但牺牲了针对性分块检索的精确度。我们通过在 FinDER 基准测试（涵盖五个组别的 1,500 个查询）上的受控评估识别出了这种鲁棒性与精确度的权衡：SFR 获得了更高的平均分（6.45 对比 6.02）和更少的失败（10.3% 对比 22.5%），而基于分块的检索 (CBR) 则产生了更多完美答案（13.8% 对比 8.5%）。为了解决这一权衡，我们提出了混合文档路由检索 (HDRR)，这是一种两阶段架构，它将 SFR 作为文档过滤器，随后在识别出的文档范围内进行基于分块的检索。HDRR 在消除跨文档混淆的同时，保留了针对性分块的精确度。实验结果表明，HDRR 在各项指标上均达到了最佳性能：平均分为 7.54（比 CBR 高 25.2%，比 SFR 高 16.9%），失败率仅为 6.4%，正确率为 67.7%（比 CBR 高 18.7 个百分点），完美答案率为 20.1%（比 CBR 高 6.3 个百分点，比 SFR 高 11.6 个百分点）。HDRR 通过在所有五个实验组中同时实现最低的失败率和最高的精确度，解决了这一权衡问题。

## Abstract
Retrieval-Augmented Generation (RAG) systems for financial document question answering typically follow a chunk-based paradigm: documents are split into fragments, embedded into vector space, and retrieved via similarity search. While effective in general settings, this approach suffers from cross-document chunk confusion in structurally homogeneous corpora such as regulatory filings. Semantic File Routing (SFR), which uses LLM structured output to route queries to whole documents, reduces catastrophic failures but sacrifices the precision of targeted chunk retrieval. We identify this robustness-precision trade-off through controlled evaluation on the FinDER benchmark (1,500 queries across five groups): SFR achieves higher average scores (6.45 vs. 6.02) and fewer failures (10.3% vs. 22.5%), while chunk-based retrieval (CBR) yields more perfect answers (13.8% vs. 8.5%). To resolve this trade-off, we propose Hybrid Document-Routed Retrieval (HDRR), a two-stage architecture that uses SFR as a document filter followed by chunk-based retrieval scoped to the identified document(s). HDRR eliminates cross-document confusion while preserving targeted chunk precision. Experimental results demonstrate that HDRR achieves the best performance on every metric: an average score of 7.54 (25.2% above CBR, 16.9% above SFR), a failure rate of only 6.4%, a correctness rate of 67.7% (+18.7 pp over CBR), and a perfect-answer rate of 20.1% (+6.3 pp over CBR, +11.6 pp over SFR). HDRR resolves the trade-off by simultaneously achieving the lowest failure rate and the highest precision across all five experimental groups.

---

## 论文详细总结（自动生成）

这是一份关于论文《Resolving the Robustness-Precision Trade-off in Financial RAG through Hybrid Document-Routed Retrieval》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于金融领域（如 SEC 10-K 报告）的检索增强生成（RAG）系统。
*   **研究背景**：金融监管文件具有高度的**结构同质性**（不同公司的报告章节标题、术语、表格格式几乎一致）。
*   **核心问题**：传统的“基于分块的检索”（CBR）在处理此类语料时，容易产生“跨文档分块混淆”——即查询 A 公司的信息却检索到了 B 公司语义极其相似的片段，导致回答完全错误。
*   **权衡困境**：虽然“语义文件路由”（SFR，直接定位到整个文档）能避免混淆，但由于文档过长（100-300页），会导致“上下文稀释”，降低了回答的精确度。论文旨在解决这种**鲁棒性（不犯大错）与精确度（回答完美）之间的权衡**。

### 2. 论文提出的方法论：混合文档路由检索 (HDRR)
HDRR 采用两阶段架构，结合了文档级路由的鲁棒性和分块级检索的精确度：
*   **核心思想**：先确定“在哪份文件里找”，再在“这份文件内部找”。
*   **关键技术细节**：
    1.  **第一阶段：文档路由（Stage 1）**：利用 LLM 的结构化输出功能（JSON Mode），从用户查询中提取元数据（如股票代码 Ticker 和财年 Year），将其映射到特定的文件路径。
    2.  **第二阶段：限定范围的分块检索（Stage 2）**：在第一阶段锁定的文档内部，执行混合检索（关键词 FTS5 + 向量搜索 FAISS），随后进行 RRF 融合和神经重排序（Jina Reranker v2）。
    3.  **回退机制（Fallback）**：如果路由阶段未能识别出具体文档，系统会自动降级为全局分块检索，确保系统的可用性。

### 3. 实验设计
*   **数据集**：使用 **FinDER** 基准测试集，这是专门为金融问答设计的 RAG 评估集，包含 5,703 个真实分析师查询。
*   **实验场景**：针对 S&P 500 公司的 10-K 年度报告进行问答。
*   **对比方法**：
    1.  **CBR (Chunk-Based Retrieval)**：传统的全局分块检索（基准）。
    2.  **SFR (Semantic File Routing)**：直接将整个 PDF 文档作为上下文输入给长文本 LLM。
    3.  **HDRR (本文方法)**：混合路由检索。
*   **评估指标**：采用 LLM-as-a-judge（GPT-4.1）评分（1-10分），统计平均分、失败率（1分率）、正确率（≥8分）和完美率（10分）。

### 4. 资源与算力
*   **模型使用**：生成模型和路由模型均使用 **GPT-4.1**（API 调用）。
*   **重排序模型**：使用 Jina Reranker v2（278M 参数）。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究主要基于现有的 LLM API 和向量数据库（FAISS/SQLite）进行推理和检索实验，不涉及大规模模型预训练，因此算力消耗主要集中在 API 调用和索引构建上。

### 5. 实验数量与充分性
*   **实验规模**：从 FinDER 中通过分层随机采样生成了 **5 个独立测试组**，每组 300 个查询，共计 **1,500 个查询**。
*   **充分性与公平性**：
    *   实验在 5 个组别上重复进行，结果显示出高度的一致性（标准差较小），证明了结论的稳定性。
    *   所有方法使用相同的生成模型（GPT-4.1）和评估标准，确保了对比的公平性。
    *   涵盖了定性与定量、单步与多步推理等多种金融问答场景，实验设计较为充分。

### 6. 主要结论与发现
*   **HDRR 全面胜出**：HDRR 在所有指标上均显著优于 CBR 和 SFR。其平均分达到 7.54（比 CBR 高 25.2%），失败率仅为 6.4%（比 CBR 降低了 71.6%）。
*   **解决了权衡问题**：HDRR 既拥有 SFR 的低失败率（避免跨文档混淆），又拥有 CBR 的高完美率（避免上下文稀释）。
*   **分布偏移**：SFR 虽然减少了低分，但由于“迷失在中间”现象，其得分集中在中间地带；而 HDRR 成功将得分分布推向了高分段（完美率达 20.1%）。

### 7. 优点
*   **架构优雅**：通过简单的路由逻辑解决了 RAG 在特定垂直领域（同质化语料）的顽疾。
*   **成本效益高**：相比于 SFR 动辄输入 10 万+ Token 的高昂成本，HDRR 仅需额外一次微小的路由调用，生成阶段的 Token 消耗与 CBR 持平。
*   **鲁棒性强**：设计了完善的回退机制，即使路由失败也不会比基础 RAG 更差。

### 8. 不足与局限
*   **依赖命名规范**：该方法高度依赖语料库的“命名规则性”（如必须有明确的 Ticker 和年份），对于杂乱无章、缺乏元数据的文档集合适用性受限。
*   **路由错误风险**：如果 LLM 在第一阶段提取了错误的 Ticker（如公司更名或缩写歧义），且该错误路径恰好存在，系统可能会在错误的文档中进行精确检索。
*   **多文档查询挑战**：虽然文中提到可以扩展到跨公司对比查询，但实验主要集中在单文档问答，对于需要跨数十份文档进行聚合分析的复杂查询，其表现尚待进一步验证。

（完）
