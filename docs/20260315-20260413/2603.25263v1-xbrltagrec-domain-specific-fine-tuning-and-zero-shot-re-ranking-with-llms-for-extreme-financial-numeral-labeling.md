---
title: "XBRLTagRec: Domain-Specific Fine-Tuning and Zero-Shot Re-Ranking with LLMs for Extreme Financial Numeral Labeling"
title_zh: XBRLTagRec：基于大语言模型的领域特定微调与零样本重排序，用于极端金融数值标注
authors: "Gang Hu, Qun Zhang, Jingyao Luo, Yile Jiang, Jing Chai, Haiyan Ding"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25263v1"
tags: ["query:bk-llm"]
score: 9.0
evidence: 自动化财务数字标记与XBRL文档处理
tldr: 本研究针对XBRL财务报表标签选择中分类体系庞大且标签语义相似度高的问题，提出了XBRLTagRec框架。该框架结合了领域微调与零样本重排序技术，利用微调后的FLAN-T5-Large模型生成语义标签文档并进行初步检索，再通过ChatGPT-3.5进行零样本重排序以确定最优标签。实验证明，该方法在FNXL数据集上显著优于现有SOTA模型，有效提升了复杂财务数据标注的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25263v1/fig-001.webp\", \"caption\": \"Fig. 2. Our proposed XBRLTagRec framework consists of three components: (A) Tag Document Generation, (B) Semantic Similarity Retrieval, and (C) LLM Zero-Shot Re-Ranking. First, a FLAN-T5 LoRA-tuned LLM generates a tag document from instruction prompts, financial text, and related numeric questions. Next, the Sentence-T5-XXL model encodes this document and all ground-truth XBRL documents to retrieve the Top-10 most similar ones via cosine similarity. Finally, ChatGPT-3.5 performs multi-round re-ranking of these candidates, and the final XBRL tags are selected by majority vote.\", \"page\": 2, \"index\": 1, \"width\": 1062, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.25263v1/fig-002.webp\", \"caption\": \"TABLE II THE IMPACT OF ITERATION RANGE ON XBRLTAGREC’S RE-RANKING MODULE. THE BEST RESULT IS SHOWN IN BOLD.\", \"page\": 5, \"index\": 2, \"width\": 506, \"height\": 271}]"
motivation: 现有的XBRL标签自动标注方法在面对大规模分类体系和高度相似的财务标签时，难以实现精准区分和匹配。
method: 提出XBRLTagRec框架，利用微调的FLAN-T5生成语义文档进行候选检索，并结合ChatGPT-3.5进行零样本重排序。
result: "在FNXL数据集上的实验显示，该框架在Hits@1和Macro指标上比现有SOTA模型FLAN-FinXC提升了2.64%至4.47%。"
conclusion: XBRLTagRec通过结合生成式微调与大模型重排序，为大规模且语义复杂的财务数值标注任务提供了高效且准确的解决方案。
---

## 摘要
上市公司必须根据美国证券交易委员会（SEC）的规定和公认会计原则（GAAP）披露财务信息。可扩展商业报告语言（XBRL）作为一种基于 XML 的财务语言，实现了标准化和机器可读的报告，但从庞大的分类标准中准确选择标签仍然具有挑战性。现有的基于微调的方法难以区分高度相似的 XBRL 标签，限制了其在财务数据匹配中的性能。为了解决这些问题，我们提出了 XBRLTagRec，这是一个用于自动化金融数值标注的端到端框架。该框架利用微调后的 FLAN-T5-Large 模型生成语义标签文档，通过语义相似度检索相关候选标签，并应用 ChatGPT-3.5 进行零样本重排序以选择最优标签。在 FNXL 数据集上的实验表明，XBRLTagRec 的表现优于目前最先进的 FLAN-FinXC 框架，在 Hits@1 和 Macro 指标上实现了 2.64%-4.47% 的提升。这些结果证明了其在大规模且语义复杂的标签匹配场景中的有效性。

## Abstract
Publicly traded companies must disclose financial information under regulations of the Securities and Exchange Commission (SEC) and the Generally Accepted Accounting Principles (GAAP). The eXtensible Business Reporting Language (XBRL), as an XML-based financial language, enables standardized and machine-readable reporting, but accurate tag selection from large taxonomies remains challenging. Existing fine-tuning-based methods struggle to distinguish highly similar XBRL tags, limiting performance in financial data matching. To address these issues, we introduce XBRLTagRec, an end-to-end framework for automated financial numeral tagging. The framework generates semantic tag documents with a fine-tuned FLAN-T5-Large model, retrieves relevant candidates via semantic similarity, and applies zero-shot re-ranking with ChatGPT-3.5 to select the optimal tag. Experiments on the FNXL dataset show that XBRLTagRec outperforms the state-of-the-art FLAN-FinXC framework, achieving 2.64%-4.47% improvements in Hits@1 and Macro metrics. These results demonstrate its effectiveness in large-scale and semantically complex tag matching scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《XBRLTagRec: Domain-Specific Fine-Tuning and Zero-Shot Re-Ranking with LLMs for Extreme Financial Numeral Labeling》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在庞大且语义高度重叠的 XBRL（可扩展商业报告语言）分类体系中，为财务报表中的数值准确分配标准化的标签。
*   **研究背景**：上市公司需按 GAAP 准则披露财务数据，XBRL 标签化是实现机器可读性的关键。然而，XBRL 标签系统规模极大（极端分类问题），且许多标签定义极其相似（如不同类别的“公允价值”），导致现有基于命名实体识别（NER）或简单微调的方法在区分细微语义差别时表现不佳。
*   **研究动机**：现有的生成式模型虽然能提供候选标签，但缺乏有效的语义排序机制来从相似候选中筛选出最优解。

### 2. 论文提出的方法论：XBRLTagRec 框架
该框架采用“生成-检索-重排序”的端到端流程，包含三个核心模块：
*   **标签文档生成 (Tag Document Generation)**：
    *   使用 **FLAN-T5-Large** 作为基础模型，通过 **LoRA (Low-Rank Adaptation)** 进行参数高效微调。
    *   输入包含指令（Prompt）、财务文本上下文和针对特定数值的问题。
    *   核心思想是让模型生成描述性的“标签文档”而非简单的标签 ID，以捕获更丰富的语义结构。
*   **语义相似度检索 (Semantic Similarity Retrieval)**：
    *   利用预训练的 **Sentence-T5-XXL** 模型将生成的文档和所有标准库中的 XBRL 标签文档转化为向量。
    *   通过**余弦相似度**计算，从数千个标签中检索出 Top-10 候选标签。
*   **LLM 零样本重排序 (LLM Zero-Shot Re-Ranking)**：
    *   引入 **ChatGPT-3.5** 作为重排序器。
    *   **关键技术细节**：采用“过滤+多轮迭代+频率投票”策略。将 Top-10 候选分为两组，每组 5 个，让 LLM 在组内进行相关性排序；通过多轮随机打乱顺序的迭代（通常 8 轮），统计每个标签被选为 Top-1 的频率，最终通过**多数投票制**确定最终标签。

### 3. 实验设计
*   **数据集**：使用 **FNXL 数据集**（基于 SEC 10-K 申报文件），包含 2,339 家公司、约 14.3 万个标注数值实例，涉及 2,794 个不同标签，具有明显的长尾分布特征。
*   **Benchmark（评价指标）**：Hits@1（首选准确率）、Macro-Precision、Macro-Recall、Macro-F1（宏平均指标，用于衡量类别不平衡下的表现）。
*   **对比方法**：
    *   **FLAN-FinXC**（之前的 SOTA 生成式模型）。
    *   **AttentionXML**（基于 BERT 的极端多标签分类流水线）。
    *   **FiNER**（基于 NER 的序列标注方法）。
    *   **GalaXC**（基于图神经网络的方法）。
    *   **Label Semantics**（结合标签语义描述的方法）。

### 4. 资源与算力
*   **硬件环境**：使用了 **1 台 32GB Tesla V100 GPU**。
*   **训练细节**：
    *   微调 FLAN-T5-Large 时，LoRA 的秩（rank）设为 2，alpha 设为 32。
    *   训练 5 个 Epoch，学习率为 5e-4，Batch Size 为 8。
    *   重排序阶段调用了 `gpt-3.5-turbo-1106` 的 API。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主对比实验**：在全量测试集上对比了 5 种基准模型。
    *   **参数敏感性实验（消融实验）**：针对迭代轮数（1-13 轮）、分组大小（3-7 个）、分组策略（保序 vs 随机）进行了多组对比。
    *   **泛化性实验**：更换了重排序的后端 LLM（包括 GPT-4, DeepSeek-V3, ERNIE-3.5-8K）以验证框架的通用性。
*   **充分性评价**：实验设计较为全面，不仅验证了最终效果，还深入探讨了重排序机制中各参数对性能的影响，证明了结果的客观性和框架的鲁棒性。

### 6. 论文的主要结论与发现
*   **性能提升**：XBRLTagRec 在所有指标上均刷新了纪录。相比 SOTA 模型 FLAN-FinXC，Hits@1 提升了 **2.64%**，Macro-F1 提升了 **4.34%**。
*   **重排序的价值**：实验证明，单纯依靠生成模型可能在 Top-10 中包含正确答案，但 Top-1 准确率不足。通过 LLM 的语义推理进行重排序，能有效识别极度相似标签之间的细微差别。
*   **参数优化**：发现迭代 8 轮左右性能达到饱和，分组大小为 5 时在成本与精度间平衡最好。

### 7. 优点（亮点）
*   **模块化设计**：框架高度可插拔，生成器、编码器和重排序器均可根据需求更换（如使用国产大模型）。
*   **创新的重排序策略**：引入频率投票机制，有效缓解了 LLM 在长文本排序中的位置偏见（Position Bias）和随机性问题。
*   **语义深度利用**：通过生成“标签文档”而非“标签名”，充分挖掘了 XBRL 体系中的文本描述信息。

### 8. 不足与局限
*   **成本问题**：多轮迭代调用 ChatGPT API 会显著增加推理成本和时间延迟，在大规模实时标注场景下可能存在瓶颈。
*   **Embedding 依赖**：论文未深入探讨不同的向量化模型（Embedding Models）对第一阶段检索召回率的影响。
*   **零样本局限**：虽然重排序是零样本的，但第一阶段仍依赖于特定领域的微调，对于完全没有标注数据的极稀有标签，性能可能受限。

（完）
