---
title: "Relational Probing: LM-to-Graph Adaptation for Financial Prediction"
title_zh: 关系探测：用于金融预测的语言模型到图适配
authors: "Yingjie Niu, Changhong Jin, Rian Dolphin, Ruihai Dong"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10212v1"
tags: ["query:bk-llm"]
score: 9.0
evidence: 用于金融预测和股票趋势预测的大模型到图的适配
tldr: 针对金融预测中语言模型构建关系图时存在的自回归解码开销大、图构建与下游任务脱节的问题，本文提出“关系探测（Relational Probing）”方法。该方法通过在小语言模型上添加关系头，直接从隐藏状态诱导关系图，并与下游股票预测模型联合训练。实验证明，该方法在保持较低推理成本的同时，显著提升了金融预测的准确性与结构化表征能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10212v1/fig-001.webp\", \"caption\": \"Table 3: Different architectures of relation head.\", \"page\": 8, \"index\": 1, \"width\": 602, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.10212v1/fig-002.webp\", \"caption\": \"Figure 1: End-to-End Language Model → Relational Probing → GAT framework for Financial Trend Prediction: (a) News encoding; (b) A lightweight Relation Head (RH) maps language-model hidden states to adjacency matrices; (c) The GAT uses the dynamic graphs with node features to predict next-step trends.\", \"page\": 4, \"index\": 2, \"width\": 764, \"height\": 338}]"
motivation: 现有的基于提示的图构建方法存在自回归解码效率低且图生成过程与下游预测任务优化脱节的问题。
method: 提出关系探测技术，通过关系头直接从语言模型隐藏状态诱导关系图，并与下游任务模型进行端到端联合训练。
result: 在基于Qwen3系列小语言模型的实验中，该方法在股票趋势预测任务上表现出优于共现基准模型的性能。
conclusion: 关系探测实现了语言模型输出向任务特定图结构的有效转换，在提升预测精度的同时兼顾了推理效率与模型可解释性。
---

## 摘要
语言模型可用于识别文本中金融实体之间的关系。然而，尽管目前已存在结构化输出机制，基于提示（prompting）的流水线仍会产生自回归解码成本，并导致图构建与下游优化脱节。我们提出了“关系探测”（Relational Probing），该方法将标准的语言模型头替换为关系头，直接从语言模型的隐藏状态中诱导出关系图，并与用于股票趋势预测的下游任务模型进行联合训练。这种方法既能学习语义表示，又能保留所诱导关系图的严格结构。它使语言模型的输出能够超越文本，从而被重塑为适用于下游模型的特定任务格式。为了提高可复现性，我们为小语言模型（SLM）提供了一个操作性定义：即在指定的批大小和序列长度设置下，可以在单个 24GB GPU 上进行端到端微调的模型。实验采用 Qwen3 骨干网络（0.6B/1.7B/4B）作为上游 SLM，并与共现基准进行了对比。结果表明，关系探测在保持竞争力推理成本的同时，带来了持续的性能提升。

## Abstract
Language models can be used to identify relationships between financial entities in text. However, while structured output mechanisms exist, prompting-based pipelines still incur autoregressive decoding costs and decouple graph construction from downstream optimization. We propose \emph{Relational Probing}, which replaces the standard language-model head with a relation head that induces a relational graph directly from language-model hidden states and is trained jointly with the downstream task model for stock-trend prediction. This approach both learns semantic representations and preserves the strict structure of the induced relational graph. It enables language-model outputs to go beyond text, allowing them to be reshaped into task-specific formats for downstream models. To enhance reproducibility, we provide an operational definition of small language models (SLMs): models that can be fine-tuned end-to-end on a single 24GB GPU under specified batch-size and sequence-length settings. Experiments use Qwen3 backbones (0.6B/1.7B/4B) as upstream SLMs and compare against a co-occurrence baseline. Relational Probing yields consistent performance improvements at competitive inference cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Relational Probing: LM-to-Graph Adaptation for Financial Prediction》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：股票市场预测不仅依赖于个股动态，还受公司间复杂关系（如供应链、行业竞争、持股关系）的影响。
*   **核心问题**：现有的利用大语言模型（LLM）构建金融关系图的方法主要依赖“提示词（Prompting）+ 自回归解码”，存在三大缺陷：
    1.  **高延迟与高成本**：自回归生成文本速度慢且计算开销大。
    2.  **任务脱节**：图构建过程与下游预测任务（如股票趋势预测）是分离的，无法针对最终目标进行端到端优化。
    3.  **结构脆弱性**：LLM 生成的文本格式（如 JSON）容易出错，解析困难。
*   **整体含义**：本文旨在通过一种“探测（Probing）”机制，将语言模型的隐藏状态直接转化为结构化的图，实现语义理解与金融预测任务的深度对齐。

### 2. 方法论：关系探测（Relational Probing）
*   **核心思想**：舍弃 LLM 原有的文本生成头，替换为一个轻量级的“关系头（Relation Head）”，直接从模型最后一层的隐藏向量中计算实体间的交互得分，从而诱导生成邻接矩阵。
*   **关键技术流程**：
    1.  **新闻编码**：使用小语言模型（SLM）对新闻文本进行编码，获取每个 Token 的隐藏状态 $H$。
    2.  **关系诱导（Relation Head）**：
        *   定义可学习的股票代码嵌入（Ticker Embeddings）。
        *   通过**交叉注意力机制**，让股票嵌入与新闻 Token 状态交互，提取新闻中关于特定公司的语义特征。
        *   使用**双线性映射（Bilinear Map）**计算任意两家公司之间的交互得分，生成每日的关系强度矩阵。
    3.  **图构建**：对交互得分进行阈值处理和聚合，生成稀疏的每日动态关系图。
    4.  **下游预测与联合训练**：将生成的图输入图神经网络（GAT），结合历史交易数据预测股票涨跌。整个系统（SLM + 关系头 + GAT）通过预测误差进行**端到端联合训练**。

### 3. 实验设计
*   **数据集**：使用包含完整股票代码标签的金融新闻数据集，结合相应的股票市场交易数据。
*   **任务目标**：三分类股票趋势预测（上涨、下跌、持平）。
*   **基准模型（Benchmarks）**：
    *   **Majority-only**：总是预测出现频率最高的类别（中性）。
    *   **Co-occurrence**：基于新闻中公司共同出现的频率构建图的传统方法。
*   **对比方法**：使用了不同参数规模的 Qwen3 系列模型（0.6B、1.7B、4B）作为骨干网络进行对比。

### 4. 资源与算力
*   **算力定义**：论文提出了 SLM 的操作性定义，即能在**单张 24GB 显存 GPU**（如 NVIDIA RTX 4090）上完成端到端微调的模型。
*   **硬件使用**：所有实验均在 **NVIDIA GeForce RTX 4090** 上完成。
*   **训练细节**：使用 Adam 优化器，学习率在 $\{10^{-3}, 10^{-4}, 10^{-5}\}$ 间搜索，训练最多 30 个 Epoch 并采用早停机制。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对三个不同规模的模型（0.6B, 1.7B, 4B）进行了主实验。
    *   每组实验均运行 **5 次独立随机实验**并取平均值，保证了结果的统计可靠性。
    *   **消融实验**：对比了不同的关系头架构（Pooling vs. Limited Attention vs. Full Attention）以及是否包含模型生成的 Token（Input-only vs. Input+Gen）。
*   **充分性评价**：实验设计较为客观、公平。通过固定下游 GAT 架构和数据分割，确保了性能提升完全源于“关系探测”诱导出的高质量图结构。

### 6. 主要结论与发现
*   **性能提升**：关系探测生成的图在 Accuracy、Macro F1、MCC 和 AUC 等指标上全面优于传统的“共现”基准。
*   **规模效应**：随着 SLM 参数量的增加（从 0.6B 到 4B），预测性能持续提升，但提升幅度呈现边际递减效应。
*   **效率优势**：相比于生成式方法，直接从隐藏状态提取关系避免了昂贵的自回归解码，且 Input-only 模式比包含生成 Token 的模式快数十倍，且效果更好。

### 7. 优点与亮点
*   **端到端对齐**：首次实现了从原始文本到金融图结构再到预测目标的端到端梯度优化，使图结构更符合预测任务的需求。
*   **高效性**：通过“探测”而非“生成”的方式，极大地降低了在实时金融新闻流处理中的推理延迟。
*   **可复现性**：明确定义了 SLM 的硬件边界（单卡 24GB），对资源受限的学术研究和工业部署具有很强的指导意义。

### 8. 不足与局限
*   **类别不平衡**：尽管方法有效，但在极度不平衡的金融数据中，MCC 和 AUC 的绝对值仍然较低，反映了金融预测本身的极高难度。
*   **模型覆盖**：实验仅限于 Qwen3 一个模型家族，尚未验证在其他架构（如 Llama 或 Mistral）上的泛化性。
*   **图类型限制**：目前主要诱导的是同质图，对于更复杂的异质金融关系（如高管关联、行业分类等）尚未深入探讨。

（完）
