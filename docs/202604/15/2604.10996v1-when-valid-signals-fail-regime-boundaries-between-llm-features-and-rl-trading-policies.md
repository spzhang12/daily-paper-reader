---
title: "When Valid Signals Fail: Regime Boundaries Between LLM Features and RL Trading Policies"
title_zh: 当有效信号失效时：大语言模型特征与强化学习交易策略之间的机制边界
authors: Zhengzhe Yang
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10996v1"
tags: ["query:bk-llm"]
score: 9.0
evidence: 用于强化学习交易策略的大模型特征提取
tldr: "本研究探讨了利用大语言模型（LLM）提取新闻特征以增强强化学习（RL）交易智能体的有效性。通过构建自动化提示词优化框架，研究者成功提取了具有高预测性（IC > 0.15）的特征。然而，实验发现这些有效特征在宏观经济冲击导致的分布偏移下会引入噪声，导致智能体表现劣于仅使用价格数据的基准模型，揭示了特征有效性与策略鲁棒性之间的脱节。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10996v1/fig-001.webp\", \"caption\": \"Figure 5: Abbreviated visualization of v1-stable-core. Few-shot calibration examples anchoring numerical output ranges proved necessary to maximize IC.\", \"page\": 6, \"index\": 1, \"width\": 950, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2604.10996v1/fig-002.webp\", \"caption\": \"Figure 1: Cumulative portfolio value across 2025. Solid lines: 5-seed mean; shaded: ±1 std. H1 (red) captures a tariff-driven shock; H2 (yellow) is calmer. The regime split isolates when LLM features succeed and fail.\", \"page\": 2, \"index\": 2, \"width\": 932, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2604.10996v1/fig-003.webp\", \"caption\": \"Figure 6: Feature importance from a gradient-boosted tree on 5-day forward returns. Macro features dominate (credit_spread 28%, VIX 16%, treasury 14%) despite zero cross-sectional IC.\", \"page\": 7, \"index\": 3, \"width\": 444, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.10996v1/fig-004.webp\", \"caption\": \"Table 7: Transaction cost sensitivity (2025 H1, seed 42). The baseline advantage over LLM+Macro is robust across all cost levels, confirming the H1 null result is not a friction artifact.\", \"page\": 8, \"index\": 4, \"width\": 846, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.10996v1/fig-005.webp\", \"caption\": \"Table 6: H2 2025 held-out test (N=5 seeds). Mean±std. ∆Sharpe vs. Baseline.\", \"page\": 8, \"index\": 5, \"width\": 923, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.10996v1/fig-006.webp\", \"caption\": \"Table 1: Feature Value Distributions (2023-2024 training period)\", \"page\": 4, \"index\": 6, \"width\": 1013, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.10996v1/fig-007.webp\", \"caption\": \"Table 2: Prompt mutation results. Top: January 2025 optimization set (769 bundles, 38 tickers). Bottom: February 2025 out-of-sample validation.\", \"page\": 5, \"index\": 7, \"width\": 810, \"height\": 464}]"
motivation: 探索LLM生成的连续数值特征能否提升强化学习交易智能体的性能，并解决特征提取与下游任务目标不一致的问题。
method: 构建了一个模块化流水线，利用冻结的LLM作为特征提取器，并通过针对信息系数（IC）优化的自动提示词循环来提取预测性特征。
result: 优化后的提示词提取到了具有显著预测能力的特征，但在宏观经济波动期间，这些特征反而降低了交易策略的鲁棒性。
conclusion: 即使LLM提取的中间特征在统计上有效，在分布偏移环境下也未必能转化为下游策略的性能提升，强调了金融场景中鲁棒性的挑战。
---

## 摘要
大语言模型（LLM）能否生成连续的数值特征，从而提升强化学习（RL）交易智能体的表现？我们构建了一个模块化流水线，其中冻结的 LLM 作为无状态特征提取器，将非结构化的每日新闻和监管文件转换为固定维度的向量，供下游 PPO 智能体使用。我们引入了一种自动化的提示词优化循环，将提取提示词视为离散超参数，并直接针对信息系数（IC，即预测收益与实际收益之间的 Spearman 秩相关系数）而非 NLP 损失进行调优。优化后的提示词发现了具有真实预测能力的特征（在留出数据上的 IC 超过 0.15）。然而，这些有效的中间表示并不能自动转化为下游任务的性能：在由宏观经济冲击引起的分布偏移期间，源自 LLM 的特征引入了噪声，导致增强后的智能体表现逊于仅使用价格数据的基准模型。在较平稳的测试机制下，智能体表现有所回升，但宏观经济状态变量仍然是策略改进最稳健的驱动因素。我们的研究结果强调了特征级有效性与策略级稳健性之间的差距，这与分布偏移下迁移学习中已知的挑战相呼应。

## Abstract
Can large language models (LLMs) generate continuous numerical features that improve reinforcement learning (RL) trading agents? We build a modular pipeline where a frozen LLM serves as a stateless feature extractor, transforming unstructured daily news and filings into a fixed-dimensional vector consumed by a downstream PPO agent. We introduce an automated prompt-optimization loop that treats the extraction prompt as a discrete hyperparameter and tunes it directly against the Information Coefficient - the Spearman rank correlation between predicted and realized returns - rather than NLP losses. The optimized prompt discovers genuinely predictive features (IC above 0.15 on held-out data). However, these valid intermediate representations do not automatically translate into downstream task performance: during a distribution shift caused by a macroeconomic shock, LLM-derived features add noise, and the augmented agent under-performs a price-only baseline. In a calmer test regime the agent recovers, yet macroeconomic state variables remain the most robust driver of policy improvement. Our findings highlight a gap between feature-level validity and policy-level robustness that parallels known challenges in transfer learning under distribution shift.

---

## 论文详细总结（自动生成）

这篇论文探讨了在量化交易领域，如何有效地将大语言模型（LLM）生成的文本特征集成到强化学习（RL）策略中，并深入分析了“特征有效性”与“策略鲁棒性”之间的脱节现象。

### 1. 核心问题与研究动机
*   **核心问题**：LLM 提取的数值特征（如情绪、影响力）虽然在统计上具有预测性，但能否在动态的市场环境（尤其是分布偏移或宏观冲击时）中真正提升强化学习交易智能体的表现？
*   **研究背景**：目前将 LLM 用于金融决策的研究往往存在“目标不匹配”问题——LLM 通常针对 NLP 损失（如准确率）进行优化，而非下游的财务指标。此外，金融市场的非平稳性（Non-stationarity）使得在训练集有效的特征在测试集可能失效。

### 2. 方法论
论文提出了一个模块化的流水线，主要包含以下核心组件：
*   **无状态特征提取器**：使用冻结的 LLM（Qwen3-235B）作为特征提取引擎。它处理每日新闻、SEC 监管文件等非结构化文本，输出 9 维连续数值向量（包括情绪、影响力、冲突信号、新闻新颖性以及宏观指标）。
*   **提示词即超参数优化（Prompt-as-hyperparameter）**：
    *   引入自动化优化循环，使用 Claude 作为“元优化器”。
    *   **优化目标**：直接针对**信息系数（Information Coefficient, IC）**——即预测收益与实际收益之间的 Spearman 秩相关系数，而非传统的 NLP 指标。
    *   **流程**：通过“变异-评估-选择”循环，迭代改进提示词。实验发现，加入“Few-shot（少样本）校准”是提升 IC 的关键。
*   **强化学习智能体**：使用 PPO 算法。智能体的状态空间由价格数据（OHLCV）、技术指标、LLM 提取的特征和宏观变量组成。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练期**：2023–2024 年数据。
    *   **验证期（H1 2025）**：宏观冲击机制（受关税政策驱动的波动期）。
    *   **测试期（H2 2025）**：平稳机制（较平静的市场环境）。
    *   **标的**：21 只高流动性美股大盘股（如 AAPL, NVDA, TSLA 等）。
*   **对比方法（消融实验）**：
    1.  **Baseline**：仅使用价格和技术指标。
    2.  **LLM-only**：仅使用 LLM 提取的新闻特征。
    3.  **Macro-only**：仅使用宏观经济指标（VIX、国债收益率等）。
    4.  **LLM+Macro**：全特征模型。
*   **评估指标**：夏普比率（Sharpe Ratio）、累计收益、最大回撤、IC 值。

### 4. 资源与算力
*   **模型**：使用了 Qwen3-235B（通过 API）进行特征提取，Claude API 进行提示词优化。
*   **训练细节**：RL 智能体在 FinRL 框架下训练了 500,000 个时间步。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号或训练时长，但指出使用了 Go 语言构建并发数据流水线，并使用 SQLite 存储回测数据。

### 5. 实验数量与充分性
*   **实验规模**：针对 21-38 只股票进行了跨越 3 年的数据回测。
*   **充分性**：
    *   使用了 **5 个随机种子**进行重复实验并取平均值，以减少 RL 的随机性干扰。
    *   进行了**多机制消融实验**（波动期 vs. 平稳期），这在金融 AI 论文中是非常客观的做法。
    *   进行了**交易成本敏感性分析**（0bp 到 50bp），证明了结论在现实摩擦下依然成立。
*   **局限性**：5 个种子在统计学上的效能（Power）相对较低，且股票池仅限于美股大盘股，泛化性有待验证。

### 6. 主要结论与发现
*   **特征有效性不等于策略收益**：优化后的提示词能产生 IC > 0.15 的有效特征，但在宏观冲击期间（H1 2025），这些特征反而引入了噪声，导致 LLM 增强型智能体表现劣于基准模型。
*   **机制依赖性**：在平稳市场中，LLM 特征能提升表现；但在高波动市场中，宏观变量（如 VIX）才是最稳健的驱动力。
*   **宏观指标的“刹车”作用**：宏观特征虽然不具备横截面预测力（IC 为 0），但能告诉智能体何时该谨慎交易，占据了策略决策权重的 58%。
*   **信息延迟限制**：由于使用免费新闻源，LLM 特征的预测效力在 3-10 天达到峰值，而 1 天内的超额收益早已被高频交易者捕获。

### 7. 优点
*   **方法论创新**：将提示词优化与金融核心指标（IC）直接挂钩，而非盲目追求 NLP 准确率。
*   **实战导向**：明确区分了不同市场机制（Regimes）下的模型表现，揭示了 LLM 信号在极端环境下的脆弱性。
*   **架构严谨**：采用冻结 LLM + 外部 RL 的模块化设计，避免了端到端训练的高昂成本和不可解释性。

### 8. 不足与局限
*   **统计效能不足**：样本量和随机种子数量较少，部分结论（如 H2 2025 的提升）未达到显著的统计学意义（p > 0.1）。
*   **分布偏移挑战**：研究证明了模型在面对未见过的宏观冲击时依然脆弱，未能提出完美的自适应解决方案。
*   **数据滞后**：依赖每日汇总的新闻，无法捕捉日内的高频信号。

（完）
