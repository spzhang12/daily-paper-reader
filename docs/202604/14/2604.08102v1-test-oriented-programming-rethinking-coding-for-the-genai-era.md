---
title: "Test-Oriented Programming: rethinking coding for the GenAI era"
title_zh: 测试导向编程：重新思考生成式人工智能时代的编程
authors: Jorge Melegati
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08102v1"
tags: ["query:llmdev"]
score: 9.0
evidence: GenAI时代名为测试导向编程的新编程范式
tldr: 本研究针对生成式AI时代的编程模式，提出了“面向测试编程”（TOP）这一新范式。背景是LLM已具备强大的代码生成能力，但现有工具多局限于自动补全或多智能体协作。TOP范式主张开发者应从编写生产代码转向审核由自然语言生成的测试代码，将实现细节委托给LLM。通过开发概念验证工具并生成命令行程序，研究验证了该方法的可行性，并指出了其在实际应用中的潜力与挑战。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在利用大模型的能力提升编程抽象层级，解决开发者在生成式AI时代仍需过度关注生产代码实现细节的问题。
method: 提出一种名为“面向测试编程”的新范式，让开发者专注于验证自然语言生成的测试用例，而由模型自动完成生产代码。
result: 开发了概念验证工具并成功生成了一个小型命令行程序，实验结果显示该范式在不同大模型下均表现出良好的可行性。
conclusion: TOP范式为AI辅助编程提供了新视角，虽然在复杂项目中仍面临挑战，但具有重塑未来软件开发流程的潜力。
---

## 摘要
大语言模型（LLMs）在生成软件代码方面展现出了惊人的能力，从而被用于辅助开发人员进行编程。目前提出的工具要么依赖于改进自动补全的助手，要么依赖于多智能体（multi-agents）系统，在后者中，不同的模型实例被编排以执行问题的各个部分，从而达成完整的解决方案。我们认为，LLMs 可以实现更高层次的抽象，即一种我们称之为“测试导向编程”（Test-Oriented Programming, TOP）的新范式。在这一范式下，开发人员只需检查基于自然语言规范生成的测试代码，而无需关注生产代码，后者可以委托给 LLMs 完成。为了评估该提议的可行性，我们开发了一个概念验证工具，并利用它调用两种不同的 LLMs 生成了一个小型命令行程序。我们取得了令人鼓舞的结果，并识别出了在实际项目中使用该范式所面临的挑战。

## Abstract
Large language models (LLMs) have shown astonishing capability of generating software code, leading to its use to support developers in programming. Proposed tools have relied either on assistants for improved auto-complete or multi-agents, in which different model instances are orchestrated to perform parts of a problem to reach a complete solution. We argue that LLMs can enable a higher-level of abstraction, a new paradigm we called Test-Oriented Programming (TOP). Within this paradigm, developers only have to check test code generated based on natural language specifications, rather than focusing on production code, which could be delegated to the LLMs. To evaluate the feasibility of this proposal, we developed a proof-of-concept tool and used it to generate a small command-line program employing two different LLMs. We obtained promising results and identified challenges for the use of this paradigm for real projects.

---

## 论文详细总结（自动生成）

这是一份关于论文《Test-Oriented Programming: rethinking coding for the GenAI era》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在生成式人工智能（GenAI）时代，如何进一步提升编程的抽象层级？
*   **背景**：目前的 AI 辅助编程工具（如 GitHub Copilot）或多智能体系统仍要求开发者深入检查和维护**生产代码**。这类似于在汇编时代编写代码，虽然速度变快了，但抽象层级没有本质提升。
*   **动机**：作者认为 LLM 的出现使得将编程语言从“代码级”提升到“自然语言/规范级”成为可能。为了解决自然语言的歧义性，需要一种形式化的验证手段，而“测试代码”正是连接自然语言需求与自动生成生产代码的最佳桥梁。

### 2. 论文提出的方法论：测试导向编程（TOP）
*   **核心思想**：开发者不再编写或直接维护生产代码，而是将精力集中在**验证和修改由 AI 生成的测试代码**上。生产代码的实现完全委托给 LLM，并由测试代码进行约束和验证。
*   **关键流程（以 Onion 工具为例）**：
    1.  **配置阶段**：开发者编写 YAML 配置文件，包含项目目标、依赖、输出描述及自然语言形式的验收测试（Acceptance Tests）。
    2.  **结构生成**：工具调用 LLM 生成项目的包、类和方法结构（也是 YAML 格式），开发者可进行调整。
    3.  **测试生成**：工具根据结构和需求生成具体的测试代码（如 Python 的单元测试和验收测试）。
    4.  **人工验证**：开发者检查测试代码的逻辑是否符合预期。
    5.  **代码迭代生成**：工具运行测试，若失败则反馈给 LLM 重新生成生产代码，直到通过所有测试。若多次尝试失败，则提示开发者修改测试或需求。

### 3. 实验设计
*   **场景**：开发一个处理 BibTeX 条目的命令行工具，功能包括添加、列出和搜索条目，并持久化到文件。
*   **对比模型**：
    *   **OpenAI GPT-4o-mini**：代表具有较强推理能力的模型。
    *   **Google Gemini 1.5-Flash**：代表轻量级、非推理导向（或不同架构）的模型。
*   **实验过程**：针对每个模型分别进行 5 次从零开始的完整生成实验，记录开发者干预次数、代码质量、日志和 Prompt 交互。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **资源消耗**：实验主要基于 API 调用（OpenAI 和 Google 的云端模型），属于应用层研究，不涉及底层模型训练。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 10 次完整的端到端生成实验（2 个模型 × 5 次重复）。
*   **充分性评价**：
    *   作为一篇 **ICSE Companion（短篇/概念验证）** 论文，其实验规模足以证明 TOP 范式的可行性。
    *   **客观性**：作者公开了工具源码和完整的实验日志（包含所有 Prompt），具有较高的透明度。
    *   **局限性**：实验仅限于一个简单的小型命令行程序，缺乏在大规模、多模块复杂系统上的验证，实验样本量较小。

### 6. 论文的主要结论与发现
*   **可行性验证**：TOP 范式是可行的，开发者确实可以在不直接修改生产代码的情况下完成功能开发。
*   **开发者负担转移**：主要的开发压力从“写代码”转变为“审核测试代码”。
*   **模型差异**：不同模型生成的代码风格差异巨大（GPT-4o-mini 更简洁，Gemini 更冗长且注释多）。
*   **确定性问题**：LLM 的非确定性导致即使输入相同，每次生成的测试和生产代码也存在差异。

### 7. 优点（亮点）
*   **范式创新**：提出了一个符合 GenAI 特性的新编程范式，试图将开发者从繁琐的实现细节中解放出来。
*   **闭环验证**：利用测试驱动开发（TDD）的逻辑解决了 LLM 生成代码不可靠的问题，实现了“Assured LLM-based SE”。
*   **工具化落地**：开发了 Onion 工具，提供了一套可操作的 YAML 配置驱动流程。

### 8. 不足与局限
*   **验证压力**：生成的测试代码量可能非常庞大，开发者审核测试代码的负担可能并不比审核生产代码轻。
*   **复杂性扩展**：目前仅适用于单模块小项目，对于微服务或大型单体架构的适用性待考证。
*   **Prompt 敏感性**：实验发现为某一模型优化的 Prompt 在另一模型上表现不佳，存在模型锁定风险。
*   **调试困难**：如果测试代码本身存在逻辑错误，可能会导致 LLM 生成错误的生产代码，形成“垃圾进，垃圾出”的循环。

（完）
