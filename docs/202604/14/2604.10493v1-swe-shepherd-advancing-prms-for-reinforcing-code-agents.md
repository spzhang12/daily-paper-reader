---
title: "SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents"
title_zh: SWE-Shepherd：推进过程奖励模型以增强代码智能体
authors: "Mahir Labib Dihan, Md Ashrafur Rahman Khan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10493v1"
tags: ["query:llmdev"]
score: 9.0
evidence: 用于增强软件工程代码智能体的过程奖励模型
tldr: 针对大语言模型智能体在处理复杂软件工程任务时缺乏细粒度反馈的问题，本文提出 SWE-Shepherd 框架。该框架引入了过程奖励模型（PRM），通过在 SWE-Bench 轨迹上构建动作级奖励数据集并训练轻量级模型，为智能体的每一步操作提供密集监督。在推理阶段，PRM 引导智能体选择高奖励决策，显著提升了在仓库级代码编辑和导航中的效率与准确性，为强化代码智能体提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10493v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the SWE-Shepherd framework. The framework consists of six stages. (1) Training tasks are collected from SWE-Bench. (2) An LLM-based agent tries to solve the tasks by interacting with the codebase and generate solution trajectories composed of intermediate reasoning steps and actions. (3) Each intermediate step is assigned a scalar reward reflecting its contribution toward solving the task. (4) A dataset is constructed where the input text (problem description, execution history, and current action) is paired with the computed reward labels. (5) A Process Reward Model (PRM) is trained by feeding text representations from the LLM into an MLP head and optimizing with mean squared error (MSE) loss to predict step-level rewards. (6) During inference, the trained PRM guides reward-aware search, prioritizing higher-quality intermediate steps to improve problem-solving performance.\", \"page\": 1, \"index\": 1, \"width\": 499, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.10493v1/fig-002.webp\", \"caption\": \"Table 1: Performance on SWE-Bench Verified using gpt-5-mini (100 tasks, max 30 steps).\", \"page\": 2, \"index\": 2, \"width\": 506, \"height\": 405}]"
motivation: 现有代码智能体在处理长程软件工程任务时依赖静态提示，缺乏对中间决策的细粒度反馈，导致探索效率低下且错误易累积。
method: 提出 SWE-Shepherd 框架，通过 SWE-Bench 轨迹构建动作级奖励数据集，并训练轻量级过程奖励模型（PRM）在推理时评估并引导智能体决策。
result: 在 SWE-Bench Verified 基准测试中，该方法显著提升了智能体的交互效率和动作质量，证明了过程监督的有效性。
conclusion: 引入 PRM 能有效优化代码智能体的决策路径，但如何将中间奖励与最终任务成功完全对齐仍是未来研究的重要挑战。
---

## 摘要
由于需要在庞大且不断演进的代码库上进行长程推理，并在相互依赖的操作之间做出一致的决策，自动化处理现实世界的软件工程任务对于基于大语言模型（LLM）的智能体而言仍然具有挑战性。现有方法通常依赖静态提示策略或手工设计的启发式规则来选择代码编辑、文件导航和测试执行等操作，但缺乏对中间决策的细粒度反馈。这导致了低效的探索、错误传播以及脆弱的解决方案轨迹。为了解决这一局限性，我们提出了 SWE-Shepherd 框架，该框架引入了过程奖励模型（PRM），为仓库级代码智能体提供密集的步骤级监督。利用来自 SWE-Bench 的轨迹，我们构建了一个操作级奖励数据集，并在基础 LLM 上训练了一个轻量级奖励模型，用于评估中间操作的效用。在推理过程中，PRM 评估候选操作并引导智能体做出更高奖励的决策，而无需进行完整的强化学习。在 SWE-Bench Verified 上的实验证明了交互效率和操作质量的提升，同时也揭示了将中间奖励与最终任务成功对齐所面临的挑战。

## Abstract
Automating real-world software engineering tasks remains challenging for large language model (LLM)-based agents due to the need for long-horizon reasoning over large, evolving codebases and making consistent decisions across interdependent actions. Existing approaches typically rely on static prompting strategies or handcrafted heuristics to select actions such as code editing, file navigation, and test execution, but they lack fine-grained feedback on intermediate decisions. This leads to inefficient exploration, error propagation, and brittle solution trajectories. To address this limitation, we propose SWE-Shepherd, a framework that introduces Process Reward Models (PRMs) to provide dense, step-level supervision for repository-level code agents. Using trajectories from SWE-Bench, we construct an action-level reward dataset and train a lightweight reward model on a base LLM to estimate the usefulness of intermediate actions. During inference, the PRM evaluates candidate actions and guides the agent toward higher-reward decisions without requiring full reinforcement learning. Experiments on SWE-Bench Verified demonstrate improved interaction efficiency and action quality, while also highlighting challenges in aligning intermediate rewards with final task success.

---

## 论文详细总结（自动生成）

这是一份关于论文《SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents》的深度结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：现有的 LLM 代码智能体在处理复杂的软件工程任务（如修复 Bug、代码重构）时，通常依赖静态提示或简单的启发式规则。由于缺乏对中间决策（如读取哪个文件、如何修改代码、何时运行测试）的细粒度反馈，智能体容易陷入低效探索、错误累积和路径脆弱的问题。
*   **研究背景**：在仓库级（Repository-level）任务中，任务周期长且步骤间高度依赖。传统的“结果奖励”（Outcome Reward）信号过于稀疏，难以指导智能体在长程推理中做出正确选择。

### 2. 方法论：SWE-Shepherd 框架
该框架的核心思想是引入**过程奖励模型（Process Reward Models, PRMs）**，将原本稀疏的成功信号转化为密集的步骤级监督。
*   **核心流程**：
    1.  **任务与轨迹收集**：从 SWE-Bench 收集任务，利用基准 LLM 智能体生成解决问题的交互轨迹（包含动作和环境反馈）。
    2.  **启发式奖励计算**：基于启发式规则为每个中间步骤分配标量奖励。规则包括：执行是否成功、是否访问了相关文件、是否修改了目标文件、测试结果如何以及是否避免了重复操作。
    3.  **数据集构建**：将“问题描述 + 交互历史 + 当前候选动作”作为输入，与计算出的奖励标签（归一化至 [0, 1]）配对，构建包含超过 15,000 个样本的数据集。
    4.  **PRM 训练**：在预训练 LLM 基础上添加一个 MLP 头部，使用 **qLoRA** 进行参数高效微调，通过均方误差（MSE）损失函数预测步骤奖励。
    5.  **奖励引导推理**：在推理时，PRM 对多个候选动作进行评分，智能体优先选择得分最高的动作，实现“奖励引导搜索”而无需复杂的强化学习（RL）。

### 3. 实验设计
*   **数据集**：使用 **SWE-Bench Verified**（包含 500 个经过验证的真实 GitHub 问题）。
*   **实验场景**：从验证集中抽取 100 个任务进行评估，限制智能体最大交互步数为 30 步。
*   **对比基准（Baselines）**：
    *   **mini-SWE-Agent**：一个强大的顺序决策智能体，不含显式搜索或奖励模型。
    *   **SWE-Search**：使用蒙特卡洛树搜索（MCTS）和迭代细化的搜索增强型框架。
    *   **SWE-Shepherd（本文方法）**：在 mini-SWE-Agent 基础上增加 PRM 评分机制。

### 4. 资源与算力
*   **模型规格**：论文中提到的模型版本为 1.5B 参数量（基于 HuggingFace 链接）。
*   **训练技术**：使用了 **qLoRA** 进行微调，这是一种节省显存的技术。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：在 100 个任务上进行了对比实验，并进行了奖励相关性分析（Resolved vs. Unresolved 任务的平均奖励对比）。
*   **充分性评价**：
    *   **客观性**：对比了当前主流的搜索类和智能体类方法，具有一定的参考价值。
    *   **局限性**：实验样本量（100 个）相对较小（仅为验证集的 20%）；缺乏针对不同基础模型规模或不同奖励函数设计的消融实验，实验深度有待加强。

### 6. 主要结论与发现
*   **效率提升**：SWE-Shepherd 将平均交互步数从 15.2 降至 **12.2**，证明 PRM 能引导智能体走更直接的解决路径。
*   **成本优势**：相比于昂贵的 MCTS 搜索（SWE-Search），SWE-Shepherd 在成本（$0.053 vs $0.274）和解决率上均表现更优。
*   **对齐挑战**：虽然效率提升，但解决率（51%）略低于基础智能体（57%）。分析发现，已解决和未解决任务的平均奖励差异极小（0.4894 vs 0.4818），说明**中间行为奖励与最终任务成功之间的相关性仍然较弱**。

### 7. 优点与亮点
*   **轻量化**：提供了一种比强化学习（RL）更简单、比 MCTS 更高效的中间决策优化方案。
*   **密集监督**：通过将长程任务分解为动作级奖励，为代码智能体提供了更细粒度的反馈机制。
*   **开源贡献**：提供了完整的代码库、数据集和预训练模型，便于社区复现。

### 8. 不足与局限
*   **奖励偏置**：当前的启发式奖励函数可能导致智能体追求“局部最优”而忽视了全局正确性（即“局部高奖励动作不一定导致最终修复”）。
*   **性能回退**：在解决率上出现了小幅下降，说明 PRM 可能会误导智能体放弃某些看似低效但必要的探索路径。
*   **泛化性验证不足**：仅在 100 个任务上测试，且未讨论在不同编程语言或更复杂仓库中的表现。

（完）
