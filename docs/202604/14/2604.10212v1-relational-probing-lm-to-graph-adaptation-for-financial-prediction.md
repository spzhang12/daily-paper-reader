---
title: "Relational Probing: LM-to-Graph Adaptation for Financial Prediction"
title_zh: 关系探测：用于金融预测的语言模型到图适配
authors: "Yingjie Niu, Changhong Jin, Rian Dolphin, Ruihai Dong"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10212v1"
tags: ["query:bk-llm"]
score: 9.0
evidence: 金融预测中的语言模型到图的适配
tldr: 本研究针对金融预测中语言模型构建关系图时存在的自回归解码慢、图构建与下游任务脱节等问题，提出了“关系探测”（Relational Probing）方法。该方法通过在语言模型隐藏层后添加关系头，直接从语义表示中诱导关系图，并与下游股票趋势预测模型进行端到端联合训练。实验表明，该方法在使用小语言模型（SLM）的情况下，能以较低推理成本显著提升预测性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10212v1/fig-001.webp\", \"caption\": \"Table 3: Different architectures of relation head.\", \"page\": 8, \"index\": 1, \"width\": 602, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.10212v1/fig-002.webp\", \"caption\": \"Figure 1: End-to-End Language Model → Relational Probing → GAT framework for Financial Trend Prediction: (a) News encoding; (b) A lightweight Relation Head (RH) maps language-model hidden states to adjacency matrices; (c) The GAT uses the dynamic graphs with node features to predict next-step trends.\", \"page\": 4, \"index\": 2, \"width\": 764, \"height\": 338}]"
motivation: 现有的基于提示的图构建方法推理成本高，且无法将图结构生成与下游预测任务进行联合优化。
method: 提出关系探测技术，用关系头替换标准语言模型输出层，直接从隐藏状态生成关系图并与下游模型同步训练。
result: 在使用Qwen3系列小语言模型的实验中，该方法在股票趋势预测任务上表现优于传统的共现基准模型。
conclusion: 关系探测实现了从文本到结构化图的高效转换，证明了小语言模型在端到端金融关系建模中的有效性。
---

## 摘要
语言模型可用于识别文本中金融实体之间的关系。然而，尽管存在结构化输出机制，基于提示（prompting）的流水线仍会产生自回归解码成本，且使图构建与下游优化脱节。我们提出了“关系探测”（Relational Probing），该方法将标准的语言模型头替换为关系头，直接从语言模型的隐藏状态中归纳出关系图，并与用于股票趋势预测的下游任务模型进行联合训练。该方法既学习了语义表示，又保留了所归纳关系图的严格结构。它使语言模型的输出能够超越文本，从而被重塑为适用于下游模型的特定任务格式。为了提高可复现性，我们为小语言模型（SLM）提供了一个操作性定义：即在指定的批大小和序列长度设置下，可以在单块 24GB GPU 上进行端到端微调的模型。实验采用 Qwen3 骨干网络（0.6B/1.7B/4B）作为上游 SLM，并与共现基准进行了对比。关系探测在具有竞争力的推理成本下，实现了持续的性能提升。

## Abstract
Language models can be used to identify relationships between financial entities in text. However, while structured output mechanisms exist, prompting-based pipelines still incur autoregressive decoding costs and decouple graph construction from downstream optimization. We propose \emph{Relational Probing}, which replaces the standard language-model head with a relation head that induces a relational graph directly from language-model hidden states and is trained jointly with the downstream task model for stock-trend prediction. This approach both learns semantic representations and preserves the strict structure of the induced relational graph. It enables language-model outputs to go beyond text, allowing them to be reshaped into task-specific formats for downstream models. To enhance reproducibility, we provide an operational definition of small language models (SLMs): models that can be fine-tuned end-to-end on a single 24GB GPU under specified batch-size and sequence-length settings. Experiments use Qwen3 backbones (0.6B/1.7B/4B) as upstream SLMs and compare against a co-occurrence baseline. Relational Probing yields consistent performance improvements at competitive inference cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Relational Probing: LM-to-Graph Adaptation for Financial Prediction》的结构化深入分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：在金融预测中，如何高效且准确地从海量新闻文本中提取动态的股票关联关系（图结构），并将其与下游预测任务（如股价趋势预测）深度整合。
*   **研究背景**：
    *   **传统方法局限**：预定义的静态图（如行业分类）无法反映市场动态；基于 Prompt 的大模型提取方法依赖自回归解码，推理成本高、速度慢，且图构建过程与下游预测任务脱节（非端到端优化）。
    *   **结构不稳定性**：LLM 在处理复杂约束时容易违反格式要求，导致解析失败。
*   **动机**：提出一种直接从语言模型（LM）隐藏状态诱导图结构的方法，实现“语言模型到图”的直接适配，并进行联合训练。

### 2. 方法论：关系探测（Relational Probing）
该方法将上游的小语言模型（SLM）与下游的图注意力网络（GAT）耦合，核心流程如下：
*   **新闻编码（News Encoding）**：使用 SLM 提取新闻文本的上下文 Token 表示（隐藏状态）。
*   **关系头（Relation Head, RH）**：
    *   **Ticker 嵌入与注意力**：初始化可学习的股票（Ticker）嵌入，通过缩放点积注意力机制（Scaled Dot-Product Attention）让股票嵌入与新闻 Token 状态进行交互，捕捉新闻中关于特定股票的语义。
    *   **交互矩阵计算**：通过双线性映射（Bilinear Map）计算股票对之间的交互得分，生成新闻级别的交互矩阵。
*   **每日图构建**：聚合当天所有新闻的交互矩阵，通过层归一化（LayerNorm）和阈值处理（Thresholding）生成稀疏的邻接矩阵和边权重。
*   **下游预测与联合训练**：
    *   **特征融合**：使用 LSTM 编码历史交易数据作为节点特征。
    *   **GAT 预测**：将诱导出的动态图输入 GAT，预测股票次日的趋势（涨、跌、平）。
    *   **端到端优化**：预测误差通过 GAT 回传至关系头及 SLM 骨干，使图构建过程直接服务于预测目标。

### 3. 实验设计
*   **数据集**：使用包含完整 Ticker 标签的金融新闻数据集，结合股票历史交易数据。
*   **任务设置**：三分类趋势预测（Positive, Neutral, Negative），基于收益率的标准差进行标注。
*   **基准方法（Benchmarks）**：
    *   **Majority-only**：总是预测多数类（Neutral）。
    *   **Co-occurrence**：基于新闻中股票共同出现的频率构建图。
*   **对比模型**：采用 Qwen3 系列（0.6B、1.7B、4B）作为 SLM 骨干网络。

### 4. 资源与算力
*   **硬件环境**：单块 **NVIDIA GeForce RTX 4090 (24GB)** GPU。
*   **SLM 定义**：论文提出了 SLM 的操作性定义，即在特定 Batch Size 和序列长度下，能在单块 24GB GPU 上完成端到端微调的模型。
*   **训练细节**：使用 Adam 优化器，学习率在 $\{10^{-3}, 10^{-4}, 10^{-5}\}$ 间搜索，训练最多 30 个 Epoch 并采用早停策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同规模的 Qwen3 模型进行了对比实验。
    *   每组实验均进行了 **5 次独立运行**并取平均值，保证了结果的统计可靠性。
*   **消融实验**：
    *   **架构消融**：对比了“全量注意力（Full）”、“受限注意力（Limited）”和“平均池化（Pooling）”三种关系头设计。
    *   **上下文消融**：对比了仅使用输入 Token（Input-only）与包含生成 Token（Input+Gen）的效果。
*   **评价**：实验设计较为充分，通过控制变量法（固定下游 GAT 和数据分割）客观地评估了图构建质量对预测性能的影响。

### 6. 主要结论与发现
*   **性能提升**：关系探测诱导的图在 Accuracy、Macro F1、MCC 和 AUC 等指标上一致优于传统的“共现基准”。
*   **规模效应**：随着 SLM 参数量增加（0.6B $\rightarrow$ 4B），预测性能持续提升，但从 1.7B 到 4B 的提升幅度放缓，存在边际效应递减。
*   **效率发现**：仅使用输入 Token 的隐藏状态（Input-only）比包含生成 Token 的方式效率高出数十倍，且性能相当甚至更好，证明了无需自回归解码即可提取高质量关系。
*   **语义价值**：简单的共现关系包含大量噪声，而关系探测能从语义层面捕捉更具预测价值的交互。

### 7. 优点与亮点
*   **端到端对齐**：解决了图构建与下游任务优化脱节的问题，使 LM 能够学习到“对预测有用的关系”。
*   **高效推理**：通过“探测”机制避开了昂贵的自回归解码过程，显著降低了延迟和计算成本。
*   **复现性强**：明确定义了 SLM 的硬件边界（单卡 24GB），为资源受限环境下的学术研究提供了参考标准。
*   **结构严谨**：通过数学推导和实证对比验证了关系头设计的合理性。

### 8. 不足与局限
*   **指标绝对值较低**：尽管有提升，但 MCC 和 AUC 的绝对值在金融预测任务中依然较低，反映了市场预测的极高难度和噪声干扰。
*   **类别不平衡风险**：虽然使用了加权损失函数，但模型在处理极度不平衡的金融数据时，准确率仍受多数类（Neutral）的主导影响。
*   **泛化性验证**：实验主要集中在 Qwen3 系列模型上，尚未在其他架构（如 Llama 或 Mistral）的小参数版本上广泛验证。
*   **应用限制**：该方法依赖于高质量的新闻流和 Ticker 标注，在缺乏实时新闻或实体识别不准的场景下效果受限。

（完）
