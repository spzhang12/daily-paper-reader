---
title: Optimizing LLM Prompt Engineering with DSPy Based Declarative Learning
title_zh: 基于 DSPy 声明式学习的大语言模型提示工程优化
authors: "Shiek Ruksana, Sailesh Kiran Kurra, Thipparthi Sanjay Baradwaj"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04869v1"
tags: ["query:llmdev"]
score: 9.0
evidence: 用于自动化、模块化和可学习提示词构建的DSPy框架
tldr: 本研究针对传统提示工程依赖人工试错、难以扩展和泛化的问题，系统探讨了基于DSPy声明式学习框架的提示优化方法。通过构建统一的DSPy架构，结合符号规划、无梯度优化和自动模块重写，实现了提示的自动合成、校准与自适应推理控制。实验证明，该方法在推理和RAG任务中显著提升了输出的可靠性与效率，为LLM系统的自动化构建提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04869v1/fig-001.webp\", \"caption\": \"Figure 2: DSPy-Based Modular Prompt Optimization Pipeline\", \"page\": 3, \"index\": 1, \"width\": 511, \"height\": 701}, {\"url\": \"assets/figures/arxiv/2604.04869v1/fig-002.webp\", \"caption\": \"TABLE I .OPTIMIZATION RESULTS\", \"page\": 5, \"index\": 2, \"width\": 505, \"height\": 275}]"
motivation: 传统的提示工程高度依赖启发式的试错过程，限制了其在复杂任务中的可扩展性、可重复性和跨模型泛化能力。
method: 提出一种统一的DSPy架构，利用符号规划、无梯度优化和自动模块重写技术，实现提示的自动化合成、纠错与校准。
result: "实验结果显示，该方法使事实准确性提升了30%至45%，并成功将幻觉率降低了约25%。"
conclusion: 基于DSPy的声明式学习框架能有效降低提示复杂性并增强模型性能，是优化大规模语言模型提示工程的高效工具。
---

## 摘要
大语言模型（LLMs）在广泛的自然语言处理任务中表现出强大的性能；然而，其有效性高度依赖于提示词的设计、结构以及嵌入的推理信号。传统的提示工程方法主要依赖于启发式的试错过程，这限制了其在不同任务间的可扩展性、可重复性和泛化能力。DSPy 是一种用于优化文本处理流水线的声明式框架，它通过为基于 LLM 的系统提供自动化、模块化且可学习的提示词构建方式，提供了一种替代方案。本文对基于 DSPy 的声明式学习在提示词优化方面进行了系统研究，重点关注提示词合成、纠错、校准和自适应推理控制。我们引入了一种统一的 DSPy LLM 架构，该架构结合了符号规划、无梯度优化和自动化模块重写，旨在减少幻觉、提高事实依据性并避免不必要的提示词复杂性。在推理任务、检索增强生成（RAG）和多步思维链（CoT）基准测试中进行的实验评估表明，该方法在输出可靠性、效率和跨模型泛化能力方面取得了一致的提升。结果显示，事实准确度提升了 30% 至 45%，幻觉率降低了约 25%。最后，我们概述了主要局限性，并讨论了声明式提示词优化框架的未来研究方向。

## Abstract
Large Language Models (LLMs) have shown strong performance across a wide range of natural language processing tasks; however, their effectiveness is highly dependent on prompt design, structure, and embedded reasoning signals. Conventional prompt engineering methods largely rely on heuristic trial-and-error processes, which limits scalability, reproducibility, and generalization across tasks. DSPy, a declarative framework for optimizing text-processing pipelines, offers an alternative approach by enabling automated, modular, and learnable prompt construction for LLM-based systems.This paper presents a systematic study of DSPy-based declarative learning for prompt optimization, with emphasis on prompt synthesis, correction, calibration, and adaptive reasoning control. We introduce a unified DSPy LLM architecture that combines symbolic planning, gradient free optimization, and automated module rewriting to reduce hallucinations, improve factual grounding, and avoid unnecessary prompt complexity. Experimental evaluations conducted on reasoning tasks, retrieval-augmented generation, and multi-step chain-of-thought benchmarks demonstrate consistent gains in output reliability, efficiency, and generalization across models. The results show improvements of up to 30 to 45% in factual accuracy and a reduction of approximately 25% in hallucination rates. Finally, we outline key limitations and discuss future research directions for declarative prompt optimization frameworks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimizing LLM Prompt Engineering with DSPy-Based Declarative Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的提示工程（Prompt Engineering）高度依赖人工启发式的“试错”过程，这种方式不仅耗时耗力，且缺乏可扩展性、可重复性，难以在不同模型间泛化。
*   **研究背景**：随着 LLM（如 GPT-4, LLaMA）的广泛应用，提示词的结构和推理信号直接影响输出质量。为了解决人工调优的局限，研究者引入了 **DSPy（Declarative Self-improving Python）** 框架，旨在将提示词从“固定的文本”转变为“可学习、可优化的参数”，实现提示工程的自动化与系统化。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用声明式编程范式，用户只需定义任务目标（Task Declaration）而非具体的提示词文本，由框架自动通过优化算法合成、测试并修订模块指令。
*   **关键技术细节**：
    *   **模块化架构**：构建了包含任务声明、检索集成、生成器（LLM）、评分模块和优化控制器的统一流水线。
    *   **优化策略**：
        *   **BootstrapOptimizer**：根据预定义规则修改和重构提示词，确保符合任务要求。
        *   **MIPRO 优化器**：在准确性、简洁性、事实正确性和效率之间寻找平衡。
    *   **自适应思维链（CoT）**：不同于静态模板，DSPy 根据任务难度和错误反馈动态增删推理步骤，避免冗余或逻辑僵化。
    *   **幻觉抑制机制**：集成检索校验、事实评分和幻觉检测器，通过自动重写提示词来纠正不支持的陈述。
*   **数学表达**：将提示词优化建模为最大化性能得分 $J(p)$ 的问题，即 $p^* = \arg \max_{p \in P} \frac{1}{N} \sum_{i=1}^N S(y_i, f_\theta(x, p))$，并在约束条件 $H(p) \le \epsilon$（幻觉率低于阈值）下进行无梯度优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **问答任务**：HotpotQA, NaturalQuestions（考察多步推理）。
    *   **推理任务**：GSM-8K（算术推理）, StrategyQA（常识推理）。
    *   **摘要任务**：XSum, CNN/DailyMail（考察长文档压缩与事实一致性）。
*   **对比方法（Baselines）**：
    *   Zero-shot（零样本提示）
    *   Few-shot（少样本提示）
    *   Chain-of-Thought (CoT)（人工编写的思维链）
    *   Self-refinement（自我修正）
    *   RAG（传统的检索增强生成）
*   **测试模型**：GPT-4-Turbo, LLaMA-3-70B, Mistral-Large。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的训练/优化时长。考虑到其使用了 GPT-4-Turbo 等闭源模型 API 以及 LLaMA-3-70B 等开源模型，实验可能涉及了高性能计算集群或云端 API 调用。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大类任务（问答、推理、摘要）共 6 个主流基准数据集，并跨越了闭源与开源三类顶级模型。
*   **充分性评价**：
    *   **较为充分**：实验不仅测量了准确率，还引入了 FactScore、幻觉率、提示词长度（效率）等维度，并提供了针对特定问题的对比数据（如 Table I）。
    *   **局限性**：虽然覆盖了多个领域，但缺乏针对不同优化器（如 Bootstrap vs MIPRO）的消融实验细节，且样本量（N）的具体数值在文中未详尽列出。

### 6. 主要结论与发现
*   **性能显著提升**：在 HotpotQA 上准确率提升约 32%，在 GSM-8K 上推理准确率提升达 45%。
*   **幻觉显著降低**：GPT-4-Turbo 的幻觉率降低了 25-30%，LLaMA-3-70B 降低了 18-22%，证明了框架的跨模型有效性。
*   **提示词效率更高**：DSPy 生成的提示词比人工编写的平均**缩短了 28%**，在保持高性能的同时降低了 Token 消耗和响应延迟。
*   **泛化能力强**：通过符号重写和简单约束，DSPy 能发现人类设计者容易忽略的潜在推理策略。

### 7. 优点（亮点）
*   **自动化转型**：成功将“直觉驱动”的提示工程转变为“原则驱动”的系统优化。
*   **模块化解耦**：将检索、生成、评分分离，使得系统易于维护和迭代。
*   **平衡性优化**：不仅追求高分，还通过约束机制兼顾了简洁性和事实可靠性。

### 8. 不足与局限
*   **计算开销**：虽然推理时提示词更短，但**优化阶段**需要多次迭代调用 LLM 进行测试和评分，这会产生较高的初始成本（Token 消耗和时间）。
*   **依赖评分函数**：优化效果高度依赖于 `Score` 模块的质量，如果评分标准本身存在偏差，优化方向可能会偏离。
*   **复杂任务的收敛性**：对于极度开放或主观的任务，如何定义客观的优化目标仍具挑战。

（完）
